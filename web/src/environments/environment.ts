export const environment = {
  production: true,
  // Direct email/password registration is disabled in production (GitHub-first).
  // Frontend mirror of the backend flag: app.auth.allow-direct-registration.
  allowDirectRegistration: false,
};

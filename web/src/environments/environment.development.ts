export const environment = {
  production: false,
  // Enabled in development so the registration flow can be tested locally
  // (the local backend profile sets app.auth.allow-direct-registration: true).
  allowDirectRegistration: true,
};
